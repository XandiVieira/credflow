spring:
  config.activate.on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5432/credflow
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: update
    show-sql: true

server:
  port: 8080